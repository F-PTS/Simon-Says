export enum MoveColors {
    Red,
    Green,
    Blue,
    Yellow,
}

export enum GameRoomState {
    Loading,
    Waiting,
    Playing,
    Full,
    Left,
    Error,
}

export enum GameResult {
    Win,
    Lose,
    Playing,
}
